# Local dev: leave EMPTY to use Vite dev proxy (relative /api → localhost:4000)
# Production (Netlify): set to your Railway API URL, e.g.
#   VITE_API_BASE_URL=https://royal-spirits-api.up.railway.app
VITE_API_BASE_URL=
VITE_EXCISE_LICENSE_NUMBER=L-EXCISE-00000
